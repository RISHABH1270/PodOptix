# Environment variables — never commit real secrets
.env

# Go build output
bin/
*.exe

# Go test cache
*.test
*.out

# IDE files
.idea/
.vscode/
*.swp

# OS files
.DS_Store
Thumbs.db
node_modules/

# Vendored Go deps — regenerated with `go mod vendor` before Docker builds.
# Keeps container builds offline (works behind corporate SSL intercept).
vendor/

# Embedded dashboard artifacts — regenerated by `cd web && npm run build`.
# Keep the folder itself (via .gitkeep) so //go:embed always finds something.
internal/dashboard/dist/*
!internal/dashboard/dist/.gitkeep
